pipeline {
    agent any
	tools {
	    maven "3.6.3"
	 	}
	stages {
	    stage('Git CheckOut') {
		    steps {
			   checkout([$class: 'GitSCM', branches: [[name: '*/dragon']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/prakashst205/sonarqube-1.git']]])
			}
		}
        stage('Clean and Install') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage ('Code Quality'){
            steps {
                    withSonarQubeEnv('sonarqube-8.9.7') {
                sh 'mvn sonar:sonar'
                }
             }
        }
		
    }
}
